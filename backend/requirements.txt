# Core Django backend
Django>=4.2.0,<5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
pymysql>=1.1.0
cryptography>=41.0.2
pyjwt>=2.8.0
bcrypt==4.0.1
python-dotenv>=1.0.0

# Data science & ML (eligibility prediction)
scikit-learn>=1.3.0
pandas>=2.0.3
numpy>=1.25.1

# For the debug script
requests>=2.31.0

# AI chatbot (optional - one of these)
openai>=1.3.0
google-generativeai>=0.3.1

# OpenCV for image processing (works fine on Windows)
opencv-python-headless>=4.8.0.74

# Reports (PDF + Excel)
reportlab>=4.0.4
openpyxl>=3.1.2

# ----- OPTIONAL: face_recognition requires dlib (needs Visual Studio C++ on Windows) -----
# The app works WITHOUT this — it falls back to an OpenCV LBP matcher.
# For best accuracy, install Visual Studio C++ Build Tools, then:
#   pip install dlib face-recognition
# Prebuilt Windows wheels are environment-specific; if pip builds from source and fails,
# use the OpenCV fallback (default) and re-enroll faces after descriptor upgrades.
