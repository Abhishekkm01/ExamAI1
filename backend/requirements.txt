# Core FastAPI backend
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
sqlalchemy>=2.0.19
pymysql>=1.1.0
cryptography>=41.0.2
pydantic>=2.0.3
pydantic-settings>=2.0.2
email-validator>=2.0.0
pyjwt>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Data science & ML (eligibility prediction)
scikit-learn>=1.3.0
pandas>=2.0.3
numpy>=1.25.1

# For the debug script
requests>=2.31.0

# AI chatbot (optional - one of these)
openai>=1.3.0
google-generativeai>=0.3.1

# OpenCV for image processing (works fine on Windows)
opencv-python-headless>=4.8.0.74

# Reports (PDF + Excel)
reportlab>=4.0.4
openpyxl>=3.1.2

# ----- OPTIONAL: face_recognition requires dlib (needs Visual Studio C++ on Windows) -----
# The app works WITHOUT this — it falls back to a simulation. Only install if you have
# Visual Studio C++ Build Tools installed and want real biometric matching.
# To install: pip install dlib==19.24.2 face-recognition==1.3.0
# Or on Windows with pre-built wheels:
#   pip install https://github.com/jloh02/dlib/releases/download/v19.24/dlib-19.24.99-cp312-cp312-win_amd64.whl
#   pip install face-recognition
